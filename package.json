{
  "name": "maybe-json",
  "version": "0.3.1",
  "description": "Google Cloud Function helper to log and transform bucket events to Promises of new json file metadata and json data",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "drpaulbrewer@eaftc.com",
  "license": "MIT",
  "dependencies": {
    "@google-cloud/storage": "^0.8.0",
    "promise-retry": "^1.1.1"
  }
}
